# _SOURCE_DIR}/ports/prebuilt/libOSMesa.a)
# chariot_external(libglu ${CMAKfile(GLOB_RECURSE SOURCES RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} *.cpp *.c *.asm)
# chariot_bin(gl_test)
# target_link_libraries(gl_test osmesa)
